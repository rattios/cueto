import { TextTransformerDirectiveDirective } from './text-transformer-directive.directive';

describe('TextTransformerDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TextTransformerDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
